$(function () {
    let text = $('#textField');

    function returnLetters(text) {
        if (text.length == 0) {
            throw new Error('The field cannot be empty');
        }

        let count = 0;

        for (let i = 0; i < text.length; i++) {
            if() {
                count++;
            } else {
                
            }
        }
    }

    returnLetters(text);
});